#!/usr/bin/env perl -w

use strict;
use warnings;

# my $contenido;
# my $path = "/home/jb/.vim/after/ftplugin/";
# foreach my $archivo (`ls $path`) {
#     chomp($archivo);
#     $contenido = `cat $path$archivo`;
#     chomp($contenido);
#     $contenido =~ s/:!/:!clear && /g;
#     print "$contenido\n";
#     system("echo '$contenido' > $path$archivo");
# }
print "Hola mundo";
